# Same two non-secret values the web app uses (see the root .env.example) --
# EXPO_PUBLIC_ vars get bundled into the app, so only put values here that
# are safe to ship on-device (same rationale as VITE_ vars on the web side).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
